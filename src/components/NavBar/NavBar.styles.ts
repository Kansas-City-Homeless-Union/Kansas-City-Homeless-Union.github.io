export const navLinks = (theme: any) => ({
  marginLeft: theme.spacing(10),
  display: "flex",
});

export const logo = {
  flexGrow: "1",
  cursor: "pointer",
};

export const link = {
  textDecoration: "none",
  color: "#1c1c1c",
  fontSize: 20,
};
